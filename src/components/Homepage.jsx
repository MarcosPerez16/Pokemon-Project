// imports here
const Homepage = () => {
  // logic here

  //the next task is that we want to grab information from the API and display the pokemon
  //this will require us to makes some API calls and use our API.js file

  return (
    <div>
      <h2>Homepage</h2>
    </div>
  );
};

export default Homepage;
